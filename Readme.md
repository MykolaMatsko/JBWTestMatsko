Basic app to parse jsom from server?
Screen with signup/login actions obtain `access_token`. Using `access_token`, fetch text from server. Count occurrence of each character in the text that you received.
